# Backend
PORT=3000

# Use the exact token endpoint from our Key File:
SIMPRO_TOKEN_URL=https://integratd.simprosuite.com/oauth2/token
SIMPRO_API_BASE=https://integratd.simprosuite.com
SIMPRO_CLIENT_ID=
SIMPRO_CLIENT_SECRET=
# Scopes (optional; leave blank if your tenant does not require them)
SIMPRO_SCOPES=
# Optional; leave empty to auto-resolve via /companies
SIMPRO_COMPANY_ID=

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
